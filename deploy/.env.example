# 部署配置模板；这些值由 Compose 注入容器。
# 管理控制台 admin 的初始密码，已初始化的数据库不会因修改此值自动重置密码。
EMQX_DASHBOARD_PASSWORD=change_me
# 数据库用户 iot 的初始密码；已有数据卷时改此值不等于改数据库内的密码。
POSTGRES_PASSWORD=change_me
# Erlang 节点间认证秘密，不是 MQTT 用户密码；使用独立随机值。
EMQX_NODE_COOKIE=change_me
